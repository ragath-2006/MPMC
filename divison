		OUTPUT 2500AD
.ORG 2000H
DATA SEGMENT
NO1 DW AAAAH
NO2 DW BBBBH
LSB DW 0000H
MSB DW 0000H
DATA ENDS
CODE SEGMENT
	ASSUME CS:CODE;DS:DATA
	MOV AX, DATA
	MOV DS, AX
	MOV DX, 0000H		
	MOV AX, NO2
 	MOV BX, NO1
	DIV BX
	MOV LSB, AX
	MOV MSB, DX
CODE ENDS
END


